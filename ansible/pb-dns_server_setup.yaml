- name: DNS Server Setup
  hosts: infra
  become: yes
  
  tasks:
    - name: Ensure apt cache is updated
      apt:
        update_cache: yes
        cache_valid_time: 3600

  roles:
    - dns_server