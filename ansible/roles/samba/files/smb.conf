[global]
   workgroup = TECHNOVA
   server string = TechNova File Server
   security = user
   map to guest = never

[share]
   path = /srv/samba/share
   browseable = yes
   read only = no
   guest ok = no
   valid users = sysadmin operator
   force group = sambashare
   create mask = 0664
   directory mask = 0775